[project]
name = "researchos"
version = "0.0.1"
description = "Code-first multi-agent research workflow system"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
